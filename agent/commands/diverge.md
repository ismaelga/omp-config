---
description: Diverge three lenses on one problem, then converge — review, bug, or design
---
Read `skill://diverge-converge`, then run it on: $ARGUMENTS

Mode comes from the argument (`review` | `bug` | `design`). None given → infer it and state
which you picked.

Check the gate before spawning. Fails the gate → say so and use one agent.
Spawn every lens in ONE batch with the identical brief. Converge on this thread.
